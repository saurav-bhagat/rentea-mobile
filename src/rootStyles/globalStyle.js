import { StyleSheet } from 'react-native';

// create common styles for color, button(touchable opacity), button text

export const globalStyles = StyleSheet.create({
	buttonStyle: {},
	buttonTextStyle: {},
});
